info:
  name: Limpar carrinho
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3030/api/cart/{{clientId}}
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve limpar carrinho", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.items).to.have.length(0);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Limpa o carrinho ativo do clientId.
