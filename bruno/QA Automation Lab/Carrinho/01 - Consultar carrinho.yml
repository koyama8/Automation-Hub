info:
  name: Consultar carrinho
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3030/api/cart/{{clientId}}
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve consultar carrinho", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.clientId).to.exist;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Consulta ou cria carrinho ativo para clientId.
