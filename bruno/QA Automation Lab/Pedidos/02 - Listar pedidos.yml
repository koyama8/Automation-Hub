info:
  name: Listar pedidos
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3030/api/orders
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve listar pedidos", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody()).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista pedidos cadastrados.
