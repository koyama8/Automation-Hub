info:
  name: Buscar pedido
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3030/api/orders/{{orderId}}
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve buscar pedido", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.items).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Busca pedido definido em orderId.
