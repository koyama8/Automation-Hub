info:
  name: Listar usuarios
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3030/api/users
  auth:
    type: bearer
    token: '{{authToken}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista usuarios autenticados. Esperado 200 OK.
