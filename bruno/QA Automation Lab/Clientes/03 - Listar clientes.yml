info:
  name: Listar clientes
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3030/api/clients
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista clientes. Aceita search e status na query string. Esperado 200 OK.
