info:
  name: Buscar cliente
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3030/api/clients/{{clientId}}
  auth:
    type: bearer
    token: '{{authToken}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Busca o cliente definido em clientId. Esperado 200 OK.
