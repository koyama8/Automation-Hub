info:
  name: Pix expirado
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:3030/api/payments/1/confirm
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve informar que o Pix expirou", () => {
          expect(res.getStatus()).to.equal(400);
          expect(res.getBody().error).to.equal("Pix expired!");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Confirma o Pix vencido de ID 1 e valida a resposta 400 Pix expired.
