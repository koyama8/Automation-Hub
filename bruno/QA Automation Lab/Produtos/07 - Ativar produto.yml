info:
  name: Ativar produto
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:3030/api/products/{{productId}}/status
  body:
    type: json
    data: |-
      {
        "status": "active"
      }
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve ativar produto", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.status).to.equal("active");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Ativa produto definido em productId.
