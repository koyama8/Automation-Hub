info:
  name: Buscar produto
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3030/api/products/{{productId}}
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve buscar produto", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.id).to.exist;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Busca produto definido em productId.
