info:
  name: Listar produtos
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3030/api/products
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve listar produtos", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody()).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista produtos cadastrados.
