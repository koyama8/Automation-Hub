info:
  name: Health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3030/api/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Verifica se a API esta disponivel. Esperado 200 OK.
