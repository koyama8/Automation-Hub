info:
  name: Deletar evidencia
  type: http
  seq: 15

http:
  method: DELETE
  url: http://localhost:3030/api/evidences/{{evidenceId}}
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve excluir evidencia", () => {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Exclui a evidencia salva em evidenceId.



