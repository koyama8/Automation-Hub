info:
  name: Evidencia inexistente 404
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:3030/api/evidences/999999
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar 404 para evidencia inexistente", () => {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Busca evidencia que nao existe para estudar 404.



