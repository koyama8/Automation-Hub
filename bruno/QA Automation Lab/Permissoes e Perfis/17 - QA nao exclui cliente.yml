info:
  name: QA nao exclui cliente
  type: http
  seq: 17

http:
  method: DELETE
  url: http://localhost:3030/api/clients/{{permissionClientId}}
  auth:
    type: bearer
    token: "{{qaToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Fluxo negativo. QA nao possui clients delete. Esperado 403 e registro PERMISSION_DENIED na auditoria.
