info:
  name: Buscar usuario gerenciado
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3030/api/permissions/users/{{managedUserId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Busca o usuario por ID e retorna perfil, status e versao.
