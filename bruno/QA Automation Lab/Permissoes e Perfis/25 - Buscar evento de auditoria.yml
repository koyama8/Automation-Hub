info:
  name: Buscar evento de auditoria
  type: http
  seq: 25

http:
  method: GET
  url: http://localhost:3030/api/permissions/audit/{{auditEventId}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Busca um evento individual e seus relacionamentos de ator e usuario-alvo.
