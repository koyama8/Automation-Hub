info:
  name: Alterar perfil com versao
  type: http
  seq: 10

http:
  method: PATCH
  url: http://localhost:3030/api/permissions/users/{{managedUserId}}/profile
  headers:
    - name: If-Match
      value: "{{managedUserVersion}}"
  body:
    type: json
    data: |-
      {
        "profile": "viewer",
        "version": {{managedUserVersion}},
        "reason": "Reduzir privilegios para validar a matriz viewer"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        if (res.getStatus() === 200) bru.setVar("managedUserVersion", String(body.data.version));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Altera QA para viewer com If-Match e salva a nova versao.
