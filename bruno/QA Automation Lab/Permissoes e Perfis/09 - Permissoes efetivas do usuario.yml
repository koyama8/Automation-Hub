info:
  name: Permissoes efetivas do usuario
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:3030/api/permissions/users/{{managedUserId}}/effective-permissions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Resolve as permissoes do usuario a partir do perfil atual.
