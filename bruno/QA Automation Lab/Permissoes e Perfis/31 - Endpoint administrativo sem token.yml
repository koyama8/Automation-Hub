info:
  name: Endpoint administrativo sem token
  type: http
  seq: 31

http:
  method: GET
  url: http://localhost:3030/api/permissions/users
  auth:
    type: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Acesso sem autenticacao. Esperado 401.
