info:
  name: Bloquear acesso
  type: http
  seq: 12

http:
  method: PATCH
  url: http://localhost:3030/api/permissions/users/{{managedUserId}}/access
  headers:
    - name: If-Match
      value: "{{managedUserVersion}}"
  body:
    type: json
    data: |-
      {
        "status": "blocked",
        "version": {{managedUserVersion}},
        "reason": "Bloqueio preventivo para validar revogacao imediata"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        if (res.getStatus() === 200) bru.setVar("managedUserVersion", String(body.data.version));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Bloqueia o usuario, incrementa authVersion e revoga todas as sessoes.
