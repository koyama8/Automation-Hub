info:
  name: Listar contratos
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3030/api/contracts
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve listar contratos", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody()).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista todos os contratos. Aceita filtros search, status e clientId via query string.
