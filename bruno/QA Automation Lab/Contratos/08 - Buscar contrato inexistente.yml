info:
  name: Buscar contrato inexistente
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3030/api/contracts/999999
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar contrato inexistente", () => {
          expect(res.getStatus()).to.equal(404);
          expect(res.getBody().error).to.equal("Contract not found!");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Valida busca de contrato inexistente. Esperado 404 Not Found.
