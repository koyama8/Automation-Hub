info:
  name: Excluir contrato
  type: http
  seq: 7

http:
  method: DELETE
  url: http://localhost:3030/api/contracts/{{contractId}}
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve excluir contrato", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().message).to.equal("Contract deleted successfully");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Exclui o contrato definido em contractId. Esperado 200 OK.
