info:
  name: Sessao atual
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3030/api/auth/me
  auth:
    type: bearer
    token: '{{authToken}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Retorna o usuario autenticado. Esperado 200 OK.
