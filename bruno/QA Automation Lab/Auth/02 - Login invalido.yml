info:
  name: Login invalido
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3030/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "qa@adminlab.com",
        "password": "senha-incorreta"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Credenciais invalidas. Esperado 401 Unauthorized.
