info:
  name: Listar cupons
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3030/api/coupons
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve listar cupons", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody()).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista todos os cupons cadastrados.
