info:
  name: Deletar cupom
  type: http
  seq: 21

http:
  method: DELETE
  url: http://localhost:3030/api/coupons/{{couponId}}
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve excluir cupom", () => {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Exclui o cupom salvo em couponId.

