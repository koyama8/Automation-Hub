info:
  name: Expirar cupom
  type: http
  seq: 20

http:
  method: PATCH
  url: http://localhost:3030/api/coupons/{{couponId}}/expire
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve expirar cupom", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody().data.status).to.equal("expired");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Expira o cupom salvo em couponId.

