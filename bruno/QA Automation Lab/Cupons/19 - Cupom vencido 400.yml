info:
  name: Cupom vencido 400
  type: http
  seq: 19

http:
  method: POST
  url: http://localhost:3030/api/coupons/validate
  body:
    type: json
    data: |-
      {
        "code": "{{expiredCouponCode}}",
        "orderId": "{{secondLimitedOrderId}}"
      }
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve rejeitar cupom vencido", () => {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Valida cupom ativo, mas vencido pela data expiresAt.
