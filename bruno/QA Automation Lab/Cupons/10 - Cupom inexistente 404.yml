info:
  name: Cupom inexistente 404
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:3030/api/coupons/validate
  body:
    type: json
    data: |-
      {
        "code": "CUPOM-NAO-EXISTE",
        "orderId": "{{orderId}}"
      }
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar 404 para cupom inexistente", () => {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Cenario negativo para cupom que nao existe.
