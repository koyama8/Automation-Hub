info:
  name: Criar pedido para limite
  type: http
  seq: 14

http:
  method: POST
  url: http://localhost:3030/api/orders
  body:
    type: json
    data: |-
      {
        "clientId": "{{clientId}}",
        "items": [
          {
            "productId": "{{productId}}",
            "quantity": 1
          }
        ],
        "notes": "Pedido para consumir limite de cupom"
      }
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        if (res.getStatus() === 201 && body?.data?.id) bru.setVar("limitedOrderId", body.data.id);
    - type: tests
      code: |-
        test("deve criar pedido para limite de uso", () => {
          expect(res.getStatus()).to.equal(201);
          expect(res.getBody().data.totalCents).to.be.greaterThan(0);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Cria pedido separado para consumir o cupom de uso unico.
