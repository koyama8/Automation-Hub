info:
  name: Listar cupons ativos
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3030/api/coupons?status=active
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve filtrar cupons ativos", () => {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody()).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Lista apenas cupons com status active.
