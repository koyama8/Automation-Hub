info:
  name: Aplicar cupom repetido 400
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:3030/api/coupons/apply
  body:
    type: json
    data: |-
      {
        "code": "{{couponCode}}",
        "orderId": "{{orderId}}"
      }
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("nao deve aplicar o mesmo cupom duas vezes", () => {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Deve ser executado depois do cenario de aplicar cupom.
