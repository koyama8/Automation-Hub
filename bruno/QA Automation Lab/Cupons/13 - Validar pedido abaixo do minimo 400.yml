info:
  name: Validar pedido abaixo do minimo 400
  type: http
  seq: 13

http:
  method: POST
  url: http://localhost:3030/api/coupons/validate
  body:
    type: json
    data: |-
      {
        "code": "{{couponMinimumCode}}",
        "orderId": "{{orderId}}"
      }
  auth:
    type: bearer
    token: '{{authToken}}'

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve rejeitar pedido abaixo do minimo", () => {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Usa couponMinimumCode para validar a regra de valor minimo do pedido.
